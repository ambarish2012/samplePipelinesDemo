language: php

php:
  - 5.6


addons:
  firefox: "48.0"

services:
  - postgres

build:
  ci:
    - echo $SHIPPABLE_POSTGRES_VERSION
    - echo ${SHIPPABLE_POSTGRES_VERSION}
    - echo $SHIPPABLE_POSTGRES_BINARY
    - echo ${SHIPPABLE_POSTGRES_BINARY}
post_ci:
    - exit 0
      
