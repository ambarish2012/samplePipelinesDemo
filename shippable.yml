language: node_js

node_js:
  - 7.4.0

build:
  ci:
    - node --version
    - sudo apt-get install npm
    - npm install npm@latest -g
    - npm -v
    - npm install react
    - npm view react version
    
      
