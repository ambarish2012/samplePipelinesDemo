language: node_js

node_js:
  - 7.4.0

build:
  ci:
    - node --version
    - npm install -g npm
    - npm install semver
    - npm --version
    - npm install react
    - npm view react version
    
      
