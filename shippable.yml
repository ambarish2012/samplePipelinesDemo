language: node_js

node_js:
  - 7.4.0

build:
  ci:
    - node --version
    - npm uninstall npm -g
    - curl -L https://www.npmjs.com/install.sh | sh
    - npm install npm@latest -g
    - npm -v
    - npm install react
    - npm view react version
    
      
